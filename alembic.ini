[alembic]
script_location = alembic
# Keep the repo root importable so env.py can import chmabapay.
prepend_sys_path = .
# sqlalchemy.url is deliberately absent. alembic/env.py reads DATABASE_URL
# through chmabapay.config, so migrations and the app can never disagree about
# which database they are pointed at.
